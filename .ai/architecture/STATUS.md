# Status Artifact

**Type:** derived summary. Subordinate to accepted decisions; never overrides them.

## Repository Version

v0.0.0

## Current Phase

Phase 0 — Repository Baseline. Governance, methodology, and collaboration artifacts established; no project functionality implemented.

## Current Objective

Complete the GHAS governance rollout decision pack via the wayfinder map ([issue #1](https://github.com/a24577t/GitHubScanner/issues/1)) — resolve the discovery frontier, then the blocked architecture decisions.

## Architecture

- **Architecture Baseline:** none published (Pre-Baseline).
- **Architecture Version:** none.
- **Domain Model / CONTEXT.md:** not yet created (created lazily by `/domain-modeling`).
- **ADRs:** none accepted (`docs/adr/` initialized empty).

## Authority Domains

- **Methodology** — `.ai/methodology/` (principles, lifecycle model, glossary, MADRs).
- **Collaboration** — `.ai/collaboration/` (load order, collaboration contract).
- **Architecture** — `.ai/architecture/` (this Status Artifact; baseline artifacts to follow).
- **Agent configuration** — `CLAUDE.md`, `docs/agents/` (issue tracker, triage labels, domain-doc rules).

## Next Milestone

Architecture Discovery Phase 1 — resolve the wayfinder discovery tickets (platform, licensing, native controls, CI/runners, estate shape) and open the blocked decision tickets.
