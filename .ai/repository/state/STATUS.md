# Status Artifact

**Type:** derived summary. Subordinate to accepted decisions; never overrides them.

## Repository Version

v0.2.0

## Current Phase

Phase 1 — Observation. Vertical Slice 1 (Observation Collector) implemented, independently gated (PASS at `d5d4572`), and merged (PR #19, merge commit `193fad1`). Validation on merged main: 39/39 tests OK, Python 3.12.10, stdlib-only. Vertical Slice 2 (Repository Configuration Collector) implemented and complete (T1–T10; completion recorded 2026-07-30, regression on the completion base `0e5a652`: 218/218 tests OK). Slice 2 Architecture Consolidation executed 2026-07-30 (owner-authorized; consolidation record `docs/consolidation/slice-2-architecture-consolidation.md`; regression on the consolidation base `0da2265`: 218/218 tests OK, docs-only diff).

## Current Objective

Execute **Slice 3 T7 (S8–S11, frontier ticket [#73](https://github.com/a24577t/GitHubScanner/issues/73) — slice completion)**, the final Slice 3 ticket, landing through ⟦G-Merge⟧. **T7 has not begun. Slice 3 is not yet Milestone-Complete** — T7 must complete its own S8–S11 verify chain and ⟦G-Merge⟧ before Milestone-Complete can be evaluated; the post-slice consolidation gate follows. No Policy Expectation / Conformance work has begun (both await the policy-definition decision). S6 `to-tickets` is complete: tickets [#67–#73](https://github.com/a24577t/GitHubScanner/issues/67) published 2026-07-31, consuming the accepted specification's T1–T7 breakdown 1:1, linear native blocking chain #67→#73, no triage labels (labeling awaits owner authorization). Every ticket carries the owner's standing implementation-review directives: conformance to ADR-0009 and the accepted specification, changes traced to T-tasks and acceptance criteria, generic engine unchanged unless explicitly authorized, architectural-behavior changes routed as halt-don't-decide events. Slice 3 — Secret Scanning Control Family — architecture is **accepted**: grill verdict PASS WITH CONDITIONS (Decision S4-SLICE3-01, ⟦G-Verdict⟧ 2026-07-30, approved with the owner-directed enablement factual correction), five binding conditions carried into the normative specification (`docs/specifications/vertical-slice-3-secret-scanning-control-family.md`) and validation matrix (V41–V62); ADR-0009 accepted; `CONTEXT.md` created under the recorded scope constraint (ADR-0008 domain concepts only). Acceptance closeout is **complete** (2026-07-31): PR #66 merged (`236fe50`), regression on merged main 218/218 tests OK, issue [#27](https://github.com/a24577t/GitHubScanner/issues/27) closed with the recorded disposition (grill condition 3 — dedicated-request decision and projection-only rejection recorded on the issue). Future slices select one or more Security Controls from the capability roadmap (`docs/planning/security-controls.md`, governed by ADR-0008) as bounded candidates, never a monolithic slice; conformance planes additionally await the policy-definition decision; the cross-evidence-shape additivity proof is assigned to the next implemented roadmap control's slice. Slice 3 T1 ([#67](https://github.com/a24577t/GitHubScanner/issues/67)), T2 ([#68](https://github.com/a24577t/GitHubScanner/issues/68)), T3 ([#69](https://github.com/a24577t/GitHubScanner/issues/69)), T4 ([#70](https://github.com/a24577t/GitHubScanner/issues/70)), T5 ([#71](https://github.com/a24577t/GitHubScanner/issues/71)), and T6 ([#72](https://github.com/a24577t/GitHubScanner/issues/72)), all closed completed, are complete through the independent Quality Gate and ⟦G-Merge⟧ (T1: PR #77, merged `0e2205f`; T2: PR #79, merged `a872b23`; T3: PR #81, merged `86039ed`; T4: PR #83, merged `1f55fe0`; T5: PR #85, merged `00ab0e8`; T6: PR #87, merged `e7ad46e`; completed-work entries below); **T7 implementation has not begun**. Architecture Consolidation for Slice 2 is **complete**: ADR-0008 (Security Control architectural seam) and MADR-0004 (canonical persistence model for review findings) accepted; Review Discipline finding-persistence clause added; carry-forward dispositions recorded in the consolidation record; closeout executed 2026-07-30 after the PR #58 merge (`8697a81`): Tickets A/B/C created as issues [#59](https://github.com/a24577t/GitHubScanner/issues/59)/[#60](https://github.com/a24577t/GitHubScanner/issues/60)/[#61](https://github.com/a24577t/GitHubScanner/issues/61), roadmap tracking issue [#62](https://github.com/a24577t/GitHubScanner/issues/62), authorized cross-references posted on #26/#27/#28 (issues open, scopes unchanged). Vertical Slice 2 — Repository Configuration Collector — is **complete**: T1–T10 per the approved specification (T1: PR #38, `50aa3e7`; T2: PR #40, `96c694a`; T3: PR #42, `881dfe7`; T4: PR #44, `57c143f`; T5: PR #46, `4216f65`; T6: PR #49, `dbf6491`; T7: PR #51, `0024652`; T8: PR #53, `557a1fc`; T9: PR #55, `4e84ecf`; T10 closed 2026-07-30 by owner execution decision via this STATUS reconciliation — S10 review PASS, independent S11 Quality Gate PASS at `0e5a652`, no production changes, no outstanding required remediation). The consolidation carry-forward inputs (T7/T8/T9 observations, QG-1/QG-2) are dispositioned in the consolidation record; QG-1/QG-2 are formally recorded disposition-preserved / content-transient per MADR-0004. The bounded-live-reconnaissance execution strategy remains owner-ratified (2026-07-29): reconnaissance runs are evidence-only per the specification's live-run classes clarification and planning [issue #47](https://github.com/a24577t/GitHubScanner/issues/47) — T9's validation run (the sole acceptance gate for matrix rows) is complete, no run scheduled without explicit owner authorization; the trial-environment registration currently carries synthetic workflow-validation values, and the V12 behavioral parent-exclusion limitation is recorded pending the trial route or wayfinder tickets #2/#3 (trial expiry 2026-08-28). The GHAS governance rollout decision pack (wayfinder map, [issue #1](https://github.com/a24577t/GitHubScanner/issues/1)) remains the standing planning objective — discovery frontier tickets #2–#6 open.

## Completed Work Items

- **Vertical Slice 1 — Observation Collector** (PR #19, merged `193fad1`): `collect`/`derive` CLI per the approved specification; evidence: red-first TDD record, two independent quality-gate rounds (FAIL → corrections QG-01..06 → PASS), /code-review Standards PASS + Spec PASS, token-secrecy and offline byte-identity proven in-suite.
- **Validation Environment** (PR #21, merged `bdb752d`, independent Quality Gate PASS at `7ea9944`): `GHScannerLab` organization with three Slice-1 fixtures; first live github.com validation run `20260725T184527Z` committed under `docs/validation/runs/`; environment documented in `docs/validation/validation-environment.md` (honest scope: Free-plan github.com API surface only). Slice 2's real-environment-validation precondition is now satisfied.
- **Methodology ratification and collaboration refresh** (2026-07-26): methodology audit against the Matt Pocock skill set; Skill Execution Map adopted as the operational execution model with its two governing invariants (`.ai/methodology/skill-execution-map.md`); repo-owner specialization skill pattern established (`docs/agents/repo-owner-skills.md`; first instance `to-spec-repo-owner`, PR #23 merged `f0ad563`); collaboration contract, load order, and Collaboration Avatar updated (PR #24, merged `e943ed1`). Slice 2 Architecture Grill completed 2026-07-26 with verdict PASS WITH CONDITIONS, owner-approved.
- **Slice 2 architecture accepted** (⟦G-Accept⟧, PR #25 merged `b346159`): ADRs 0004–0007 accepted; Vertical Slice 2 specification and normative validation matrix approved (architecture grill, 2026-07-26; consolidation by `to-spec-repo-owner`); acceptance closeout created future-work issues #26–#28 (Observation Target Model; `security_and_analysis` projection; per-ruleset detail).
- **`.ai` information-architecture migration** (PR #29, merged `872544e`; approved design 2026-07-26): audience/ownership namespaces `collaborator/`, `repository-owner/`, `repository/{methodology, state, history}`; history package `ia-redesign-2026-07` with exact legacy snapshot — **immutable as of the merge**, per its recorded boundary (the merge SHA is deliberately not embedded in the package; its README documents the self-reference convention); two dead files and one superseded draft removed (evidence in the package); all references updated atomically; post-merge validation 42/42 tests OK.
- **Methodology Release v0.2.0 closeout** (PRs #31, #32, #33, #35): MADR-0003 accepted; transfer architecture accepted — Observer and Review Evidence Package (REP) recorded as Architecture Accepted / Implementation Deferred under [issue #34](https://github.com/a24577t/GitHubScanner/issues/34); Decision-Gated Implementation Lifecycle ratified; Observer added to the controlled vocabulary; Collaboration Avatar regenerated through the readiness gate; GHAS SKU research evidence preserved under `docs/research/`.

- **Slice 2 T1 — descriptor table + `default-branch-protection` projection** (PR #38, merged `50aa3e7`; Quality Gate PASS, owner-recorded 2026-07-26; merge approved without code changes): `resources.py` declarative descriptor table with import-time integrity validation and pure projection interpreter, per the approved specification's module table; evidence: red-first TDD record (four cycles, preserved in the ticket commit message), 21 new behavioral tests at the ratified seams, validation on merged main 63/63 tests OK (zero regressions), diff scoped to the task (two new files, both ≤300 lines).

- **Slice 2 T2 — classification extracted into `taxonomy.py`** (PR #40, merged `96c694a`; Quality Gate PASS, owner-recorded 2026-07-26; merge approved without code changes): descriptor-anchored absence matching and the closed deterministic-reason set now owned by the pure `taxonomy.py`; `derive.py` retains evidence loading and invocation only; evidence: red-first TDD record (four cycles, in the ticket commit message), matrix rows V13–V20 covered at the classify seam, S10 three-axis review (Standards: two judgement calls dispositioned to T6; Spec: complete; Conformance: clean), validation on merged main 78/78 tests OK (zero regressions; the 42 Slice-1 CLI tests pass unchanged).

- **Slice 2 T3 — bounded execution waits in `transport.py`** (PR #42, merged `881dfe7`; Quality Gate PASS, owner-recorded 2026-07-26; merge approved without code changes): ADR-0007 precedence ladder at the transport seam (valid Retry-After ≤60 s, refused beyond, never clamped → single primary park `max(0, reset−now)+5 s` ≤3900 s, renewed exhaustion terminates → existing bounded retry → taxonomy), structured wait records behind a closed 10-field evidence schema plus `termination_reason` on structured results, injected clock seam; wait-termination evidence vocabulary (`rate_limit_renewed_exhaustion` / `renewed-exhaustion`) ratified through E1 and recorded as an append-only spec refinement — transport wait-evidence vocabulary only, T6 owns the derived-entry mapping; evidence: red-first TDD record (in the ticket commit messages), matrix rows V21–V29 at the transport seam with a deterministic fake clock, S10 three-axis review run twice to convergence, validation on merged main 91/91 tests OK (zero regressions; numeric envelope shapes unchanged so `collect.py`/`derive.py`/`report.py` are untouched).

- **Slice 2 T4 — canonical target discovery, evidence addressing, structural validation in `targets.py`** (PR #44, merged `57c143f`; independent Quality Gate PASS at `419833d`, owner-recorded 2026-07-27; merge approved without code changes): ADR-0005's canonical rule as the one discovery function shared by collection planning and offline rederivation (shape-valid 2xx pages, positive non-boolean integer id, structurally valid `owner/name` identity, eligibility before deduplication, first eligible occurrence per id wins, target set ascending by id; required inputs evaluated only after eligibility), annotation-safe evidence addressing (`<repo-id>[-<name-annotation>]`, annotation verbatim only under `^[A-Za-z0-9_.-]{1,100}$` with no trailing dot, never repaired), and deterministic structural-conflict detection (duplicate-ID directories and path/envelope ID disagreement; unrecognized directory names surfaced as reporting, never a third conflict class); E1 refinements Q1–Q3 ratified 2026-07-26 and recorded append-only in the spec and validation matrix (V36 "reserved" = reserved-character names; required-input usability = non-empty string; unrecognized-directory reporting — T6 inherits the distinction); evidence: red-first TDD record (four cycles, in the ticket commit messages), matrix rows V31–V36 and V38 at the targets seam, S10 three-axis review run twice to convergence, validation on merged main 115/115 tests OK (zero regressions; no existing module changed — unification of the duplicated page-usability predicate deferred to T6 by owner disposition).

- **Slice 2 T5 — fan-out collection for `default-branch-protection` in `collect.py`** (PR #46, merged `4216f65`; independent Quality Gate PASS at `cd21ada`, owner-recorded 2026-07-29; merge approved without code changes): orchestration-only fan-out consuming T4's discovery and evidence addressing (exact artifact layout proven as full-tree equality, envelope extensions `repo`/`resource`/`branch`, missing-input rule V33 at the real collection seam with degenerate values never coerced, descriptor-generic object-array drain, independent `--max-pages` per listing, orderly continuation for both bounded transport failure and filesystem write failure, token-secrecy scan over all new artifacts and output channels); collection-failure recording ratified through E1 Q1 and recorded as an append-only spec clarification (loud runtime surfacing, continuation, no synthetic raw artifact, absent artifact as the durable trace); evidence: red-first TDD record (four cycles, in the ticket commit messages, honest first-run-green disclosures), S10 three-axis review run twice to convergence (dead-import Standards finding fixed in `1909c32`), independent Quality Gate with reviewer-executed tests (fan-out 10/10, drain 3/3, full suite 128/128), validation on merged main 128/128 tests OK (zero regressions), diff scoped to the task (4 files, +457/−14); two non-blocking QG observations carried to T8's test-plan notes in the specification.

- **Slice 2 T6 — derivation: per-resource observed documents, coverage blocks, summary generalization** (PR #49, merged `dbf6491`; independent Quality Gate PASS at `d26aea9`, owner-recorded 2026-07-29; merge approved without code changes): one latest-only observed document per descriptor (`{run_id, state, coverage, repositories}`, entries ascending by id from T4's canonical discovery, per-resource rollup via the Slice 1 listing rule; input-derived values stay determined on non-collected entries), coverage qualifies-never-converts (V39, both directions), additive summary generalization (per-descriptor estate-independent aggregates and the ADR-0005 structural report with unrecognized-directories-as-reporting; Slice 1 report output untouched), and the descriptor-generic drain seam ahead of T8's architectural AC (array projection DSL deliberately left to T8's `resources.py` diff); E1 refinements Q1–Q3 ratified 2026-07-29 with pre-append consistency checks, recorded append-only (closed-set extension `raw-evidence-absent` for present-input/absent-artifact; structural conflicts derive `unknown` · `structural-conflict`, `unknown` covering conflict-indeterminacy; deterministic transport-termination mapping from retained evidence only, realizing the V23/V24/V27 pairs); evidence: red-first TDD record (four cycles plus the S10 fix commit, in the ticket commit messages, all first-run-greens disclosed), S10 three-axis review run twice to convergence (run-1 findings fixed red-first in `d26aea9`), independent Quality Gate with reviewer-executed tests (implementation tree 164/164; independently extracted base tree 128/128, proving exactly 36 new tests; 40+ adversarial probe assertions), validation on merged main 164/164 tests OK (zero regressions), diff scoped to the task (12 files, +943/−36; `collect.py`/`report.py`/`resources.py` untouched); three non-blocking QG observations recorded, the drain page-gap note carried into T8/V37 test planning alongside the two T5-carried notes — all non-blocking and authorizing no work outside their planned tickets.

- **Slice 2 T7 — report aggregates and wait visibility** (PR #51, merged `0024652`; delta-focused independent Quality Gate PASS at `4f382cc`, owner-recorded 2026-07-30; merge approved without code changes): envelope transport-evidence retention at the `collect.py` boundary, execution aggregation in the pre-approved `summary.py` split, per-descriptor report aggregates bounded by the closed state/reason vocabularies, and wait visibility realizing V30 (all categories/durations derived from retained evidence; planned never conflated with attempts; report size estate-independent, failures excepted — the Slice-1 `rate_limit` list bounded to aggregate figures under this AC). The append-only E1 envelope-retention refinement (owner-ratified 2026-07-29, recorded in the specification): `wait_records` retained verbatim in transport order behind the ratified closed 10-field schema with allowlisted headers only, `termination_reason` retained only when one exists and never synthesized, legacy absence reads as empty, V40 Slice-1 derivability and token secrecy preserved. Implementation sequence: specification refinement → envelope retention → summary aggregation → report rendering, each red-first, then the S10 three-axis review run five times to convergence. The initial independent Quality Gate FAILED at `591d413` with four crash findings F1–F4 sharing one root cause — incomplete type-deep tolerant-scan gating (malformed `termination_reason`, stray-file non-int `status`, non-dict `response_headers` reaching the shared rate-limit predicate, heterogeneous termination keys at canonical serialization). The owner-accepted delta repair at `4f382cc` changed exactly two files (`summary.py` plus its probe tests), added four red-first probes each failing with the predicted exception, and changed no valid-evidence semantics. The delta-focused independent Quality Gate PASSED: independently executed 185/185 full suite (focused 12/6/31), F1–F4 independently closed with broader adversarial variants than the committed probes, valid-evidence report JSON and Markdown byte-identical across the repair, strict T6 descriptor-artifact consumption preserved (empty diff on `projections.py`/`taxonomy.py`/`derive.py`; identical strict behavior across both SHAs), and the tolerant reporting scan's type-deep contract completed. Accepted non-blocking dispositions: refused waits surface as termination statistics, never wait figures (ADR-0007: a refusal is never slept); the additive execution keys (`refused`, `total_wait_seconds`, `retained_records`, `evidence_absent`, `missing_input`) stand as the planned≠attempts mechanics. Deferred consolidation notes carried to the post-slice consolidation gate, authorizing no work: the `_planned` discovery/path walk parallels `collect`/`projections` traversals; `SINGLE_ARTIFACTS` and `REFUSED_OUTCOMES` restate vocabulary that `transport`/`derive` expose only as literals. Validation on merged main 185/185 tests OK (zero regressions); diff scoped to the task (9 files, +748/−7; `transport.py`, `taxonomy.py`, `targets.py`, `projections.py`, `resources.py` untouched).

- **Slice 2 T8 — `repository-rulesets` second descriptor** (PR #53, merged `557a1fc`; independent Quality Gate PASS at `c472164`, owner-accepted 2026-07-30; merge approved without code changes): ADR-0004's falsifiable claim **proven** — the pre-implementation architectural proof traced the complete descriptor path (descriptor → discovery → collection → envelope → projection → derivation → summary → reporting; the only gap was the array-projection DSL that T6 had deferred to T8's `resources.py` diff), and the Quality Gate's independent falsification attempt confirmed every engine seam generic by diff, grep, and seam audit — no hidden dependency; production diff limited to `resources.py` (+51/−2, 177 lines: the declarative descriptor plus pure `length`/`items` projection kinds with deterministic `_item_key` ordering on the repositories-sort precedent; every other production file byte-identical to main). The descriptor: paginated drain of `/repos/{full_name}/rulesets?includes_parents=false` (V11's raw-URL request-contract evidence retained in the envelope; the projection pinned source-agnostic — exclusion lives in the request; behavioral exclusion remains V12/T9 live scope), no required per-repository input, no absence anchor (emptiness is a value, count 0 never absent — V07), projection count + per-ruleset `{id, name, target, enforcement, created_at}` sorted by ruleset id (V06), cap breach deriving `incomplete` · `pagination-cap` with every drained page contributing (V37), and the T5-carried coexistence and object-envelope notes plus the T6-carried page-gap disposition all pinned in tests. Red-first sequence: cycle 1 DSL (13 tests, 12 observed red), cycle 2 descriptor (5 tests, 4 observed red; six existing expectations extended for the two-descriptor table), cycles 3–4 collection and derive/report seams (14 tests, first-run greens honestly disclosed — the generic engines handling the shipped descriptor), then the S10 three-axis review converged in two runs (run-1 findings fixed at `c472164`). Test growth 185 → 218 (33 new, zero regressions). Byte-identical preservation of existing descriptor outputs proven in-suite and independently reproduced at the gate with fresh fixtures (`org.json`, `repositories.json`, `default-branch-protection.json` identical; `repository-rulesets.json` purely additive). Determinism review independently confirmed: adversarial ordering fixtures (duplicate/malformed/missing/equal ids, huge ints, non-dict items) byte-identical across repeated in-process and cross-process runs; page-order redistribution immaterial for distinct keys; the equal-key page-split tie deterministic per fixed tree, inherent to the pinned tie rule. Independent Quality Gate PASS (fresh-context reviewers at pinned `c472164`: full suite 218/218 independently executed, focused 19/6/7/7, byte-identity 1/1, Slice-1 compatibility 7+12; zero blocking findings, zero Repository Inconsistencies on any axis). Accepted observations — all non-blocking, deferred, authorizing no work: the specification's "sorted by ruleset `id`" does not record the malformed-id tiebreak (owner-routed editorial, carried to the post-slice consolidation gate); `_item_key`'s docstring silent on bool-ids sorting in the integer group; the equal-key page-split note at `_classify_drain`; and the dispositioned S10 recommendations (projection-dict shape appearing three times; `count`/`length` kind vocabulary; deliberate test-side `ITEM_SUMMARY` contract copy). Validation on merged main 218/218 OK (zero regressions); diff scoped to the task (7 files, +630/−9).

- **Slice 2 T9 — fixtures + live validation run** (PR #55, merged `4e84ecf`; independent Quality Gate PASS, owner-accepted 2026-07-30; validation-only ticket, **zero production-code changes** — the single evidence commit `433304b` sits directly on the pinned engine `d95e61e` with `src/`/`tests/` byte-identical, independently verified): owner-provisioned GHScannerLab fixtures (standard-repo classic protection with exactly one required approving review plus one repository-owned ruleset `standard-repo-baseline`; new public `unprotected-repo` and `empty-repo`; archived and fork states retained) and the standing fine-grained validation PAT; validation-grade run `20260730T162522Z` committed self-contained under `docs/validation/runs/20260730-GHScannerLab/` (14 raw evidence files under annotation-safe `<id>-<name>/` addressing, 4 observed documents, 2 reports) with `validation-environment.md` updated in the same change set (five-row fixture inventory, run record declaring grade validation, V09 standing-credential record: documented = configured = observed, divergences none — the Slice-1 credential divergence is closed and the standing rule satisfied from this run onward). V01–V12 outcomes: V01 collected with projected protection values; V02 absence pin confirmed verbatim (`Branch not protected`); V03 empty-repository response class pinned by the run (404 `Branch not found` → `inaccessible` · `absence-rule-unmatched-404`, never joining the absence set); V04/V05 corroborate V02 with no divergence; V06 count 1 with the five summary fields; V07 count 0 as a value everywhere else; V08 allowlisted headers only across 14/14 envelopes; **V10 recorded gap** (restricted PAT not provisioned this cycle; offline tests carry the live-denial scenario); V11 `includes_parents=false` proven in 5/5 retained request URLs; **V12 recorded limitation** (plan-gated organization ruleset; behavioral parent exclusion conditionally unvalidated; revisit via wayfinder #2/#3, trial route expiry 2026-08-28). Integrity: token scan clean under two independent implementations (working-tree and commit-blob level; teaching-workspace paths structurally excluded and absent from the commit); observed documents regenerate byte-identically from raw evidence alone (destructive re-derive); reports rebuilt byte-identically from retained evidence plus the pinned engine; fresh-clone reconstruction independently reproduced the suite (218/218), the regeneration, and the scan. S10 three-axis review converged in two runs (run-1 findings were verification-completeness sweeps only, both PASS). Accepted observations — all non-blocking, deferred, authorizing no work: one implementation recommendation (no offline CLI path regenerates `reports/`; reproducibility proven programmatically — carried to the post-slice consolidation gate) and four editorial notes (V10 gap record lacks the "safely provisionable" rationale clause; `unprotected-repo` inventory-row arrow could be misread against the emptiness-is-a-value rule; the closing coverage sentence slightly overstates offline coverage of V12 behavioral exclusion; the T9 commit-message re-derive phrasing does not note the reports scoping). Validation on merged main 218/218 tests OK (zero regressions); diff scoped to the task (21 files, +803/−3, all under `docs/validation/`).

- **Slice 2 T10 — slice completion** (closed 2026-07-30 by owner execution decision: completion recorded via a dedicated STATUS reconciliation branch and PR on the established reconciliation pattern — no production implementation PR required, no empty feature PR manufactured; an execution decision under the specification, not a reinterpretation of it): S10 three-axis review PASS; independent S11 Quality Gate PASS with zero Repository Inconsistencies, zero Architecture Proposals, and no required remediation — verdict and supporting evidence owner-accepted 2026-07-30. QG-1 and QG-2 accepted as non-blocking observations, carried forward as consolidation inputs, authorizing no implementation work within T10. Zero production changes; regression suite on the reconciliation base `0e5a652`: 218/218 tests OK. **Vertical Slice 2 (T1–T10) is complete.** The consolidation input set for the post-slice gate comprises the deferred non-blocking observations recorded under T7, T8, and T9 above, plus QG-1 and QG-2.

- **Slice 2 Architecture Consolidation** (2026-07-30; owner-authorized with two accepted refinements — Security Control as the first-class seam; enumerated control roadmap): ADR-0008 (Security Control architectural seam: Observation → Applicability → Operational State → Policy Expectation → Conformance; "enabled properly" is a conformance conclusion derived from evidence and policy, never a raw GitHub boolean; alert inventory out of scope) and MADR-0004 (canonical persistence model for review findings — full-fidelity committed finding records in the adjudicated change set from acceptance forward; non-retroactive; implementation vehicle deliberately unselected) accepted; Review Discipline finding-persistence clause added; Security Controls capability roadmap published (`docs/planning/security-controls.md`, eight enumerated seams); Slice 2 spec ruleset-ordering editorial and three validation-environment editorial carry-forwards resolved; all carry-forward inputs dispositioned in `docs/consolidation/slice-2-architecture-consolidation.md` (Tickets A/B/C and roadmap tracking issue proposed for closeout; #26 evaluated — decision deferred to the first org-scoped-target slice, issue open). Docs-only diff; production tree byte-identical to `0da2265`; regression 218/218 OK. Merged as PR #58 (`8697a81`); post-merge closeout complete — issues #59–#62 created, #26/#27/#28 cross-referenced; post-consolidation baseline on merged main `8697a81`: 218/218 tests OK.

- **Slice 3 architecture accepted** (⟦G-Accept⟧ via this reconciliation's acceptance PR; grill and consolidation 2026-07-30): Architecture Grill (S4) conducted under explicit owner authorization against the frozen proposal (`docs/planning/slice-3-architecture-grill.md`), all seven questions resolved; verdict **PASS WITH CONDITIONS** approved at ⟦G-Verdict⟧ as Decision S4-SLICE3-01 with the owner-directed factual correction (public-fixture enablement is a mandatory provisioning-time validation obligation — retained evidence contains no enablement operation, only affirmative `disabled` values). S5 consolidation (`to-spec-repo-owner` + `/domain-modeling`) produced: the Vertical Slice 3 specification and normative validation matrix (rows V41–V62 continuing the global sequence; unconditional/conditional/optional partition with V10/V12 recorded-gap mechanics) carrying the five grill conditions verbatim; ADR-0009 (Security Control realization model — dedicated shared-surface descriptor `security-and-analysis`, control-definition layer in `controls.py`, control-observation documents `observed/controls/<control>.json` with `{resource, state, reason}` evidence citations, evidence-plane-only rollup); `CONTEXT.md` created under the binding scope constraint (Security Control, Observation, Applicability, Applicability Chain, Operational State, Resource Descriptor); Security Controls roadmap rows assigned (Secret Scanning, Push Protection → Slice 3). Conditions binding forward: (1) same-evidence-surface additivity limit — cross-shape proof assigned to the next control slice; (2) `unavailable` excluded pending an affirmative discriminator (#2/#3); (3) issue #27 disposition at acceptance closeout; (4) canonical-pattern standing of the realization; (5) validation partition preserved, 2026-08-28 as scheduling input only.

- **Slice 3 T1 — `security-and-analysis` resource descriptor** (PR #77, merged `0e2205f` 2026-07-31; independent Quality Gate PASS at pinned `cd73faf`, verdict recorded on the PR ahead of ⟦G-Merge⟧; merge approved without code changes): the shared Secret Scanning / Push Protection evidence surface as one declarative descriptor in `resources.py` — dedicated `GET /repos/{full_name}` request (ADR-0009's dedicated-request realization; issue #27 disposition), object shape, no required per-repository inputs, no absence anchor (404 derives `inaccessible` · `absence-rule-unmatched-404` per ADR-0006, pinned at the V54 test), projecting `visibility` plus both control status fields (`security_and_analysis.secret_scanning.status`, `security_and_analysis.secret_scanning_push_protection.status`) so T5 adds no descriptor change; **zero engine change** — production diff `src/collector/resources.py` only (+33/−1; existing DSL kinds `field`/`object`, no new projection kinds, no control vocabulary — the control-plane classification is T2). Evidence: red-first TDD record (cycle 1 observed red at the resources seam — 10 of 11 tests — before the descriptor landed; cycles 2–4 pin the generic collection/derive/planned-request seams with first-run greens honestly disclosed, T8 precedent; V41 offline aspects, V50 absent-surface-projects-`unknown` never disablement per ADR-0008, and V51 unrecognized-values-verbatim pinned in-suite); pre-T1 observed documents and report outputs proven byte-identical in-suite on the T8 pattern (the new document/row purely additive; planned-request execution counts follow the descriptor table by design, pinned in `test_summary_execution.py`); S10 three-axis review run twice to convergence (run-1 Standards finding fixed — `test_collect_fanout.py` had crossed the 300-line rule at 322, split at the fixtures/tests boundary into `tests/fanout_fixtures.py`; Spec: the report-half of the preservation AC converted from disposition to in-suite proof; Conformance: all five checks PASS, no halt-don't-decide events; run 2 an independent convergence pass — every finding CLOSED, no new findings, 239/239 independently executed). Independent Quality Gate PASS (fresh-context reviewer at pinned `cd73faf`, base `8c78f16`): full suite independently executed 239/239 OK, focused seams 11/3/6/8, falsification attempt against the zero-engine-change claim failed to falsify, adversarial byte-identity probe beyond the committed preservation tests, descriptor conformance field-by-field to the specification and the ticket contract, all ten touched files ≤300 physical lines; zero Repository Inconsistencies, zero Architecture Proposals, zero Implementation Recommendations; one optional Editorial Improvement recorded (dotted-name unittest invocation of intra-tests imports requires discovery mode — pre-existing repository pattern, no action required). Accepted non-blocking notes carried to the post-slice consolidation gate, authorizing no work: the exact-count sweep recommendation and the spec-text editorial aside (both dispositioned in the S10 record). Test growth 218 → 239 (21 new, zero regressions). Validation on merged main 239/239 tests OK (zero regressions); diff scoped to the task (10 files, +550/−47; every engine module byte-identical to pre-merge main).

- **Slice 3 T2 — `controls.py` control-definition layer + `secret-scanning` control** (PR #79, merged `a872b23` 2026-07-31; independent Quality Gate PASS at pinned `0e9ffd5`, verdict recorded on the PR ahead of ⟦G-Merge⟧; merge approved without code changes): the ADR-0009 control-definition layer as one new pure module — declarative `CONTROLS` table validated loudly at import time on the descriptor-table precedent (empty table, malformed or duplicate names, unknown evidence descriptor, malformed status path, and applicability kinds outside the closed set `{"visibility", ("chain", <earlier control>)}` all rejected `ValueError`-loud); pure, deterministic, first-match-wins plane evaluation over derived descriptor entries; first control `secret-scanning` (`{name, descriptor: security-and-analysis, status_path: security_and_analysis.secret_scanning.status, applicability: visibility}`, field-by-field per the spec); closed vocabularies exact (operational `{enabled, disabled, inaccessible, unknown}` — `unavailable` excluded per grill condition 2; applicability `{applicable, applicability-unknown}`); exactly one deterministic reason per plane per entry, ambiguity degrades never concludes (ADR-0008), disablement only from affirmative exact `"disabled"`, no coercion anywhere. The chain applicability kind ships with the layer (pinned via a synthetic test definition; the shipped table carries secret-scanning only) — required by ADR-0009's additivity consequence and T5's definition-only architectural AC; the gate proved T5 empirically achievable as a control-definition-only diff producing the spec's exact chain reasons. **Zero engine change**: production diff = `src/collector/controls.py` alone (123 lines); all ten engine modules byte-identical to pre-merge main. Evidence: red-first TDD record (four cycles, reds observed as ImportError/TypeError before implementation, seam growth 14→25→35→48, disclosed in the commit messages); AC rows V43/V44 offline analogs, V47, V50–V54 covered at the controls seam — unit plus the `DerivedEntryContract` class proving the evaluator consumes exactly what `projections.resource_document` emits — with V55/V56 context pinned via the synthetic chain definition; S10 three-axis review run twice to convergence (run 1: Standards/Spec/Conformance all PASS, zero hard findings, eleven judgement findings — two applied as test strengthenings, the rest dispositioned in the `0e9ffd5` commit message; run 2: both fixes CLOSED, one verified empirically against the leak it guards, zero new hard findings). Independent Quality Gate PASS (fresh-context adversarial reviewer at pinned `0e9ffd5`: zero-engine-change and T5-definition-only falsification attempts both failed to falsify; 23 adversarial entries across both kinds and 9 malformed `resolved` variants with zero exceptions and closed-set outputs throughout; suites reviewer-executed — branch 289/289, controls seam 50/50, main 239/239 in a detached worktree; zero Repository Inconsistencies, zero Architecture Proposals, three non-blocking Implementation Recommendations routed to the consolidation list). Carried notes — all non-blocking, authorizing no work: `_NAME` regex duplication and validation-pattern harmonization → post-slice consolidation gate; the `resolved` table-order orchestration contract → confirm at T3's derive seam; name-derived chain reasons → note for any future second chain. Test growth 239 → 289 (50 new, zero regressions). Validation on merged main 289/289 tests OK (zero regressions); diff scoped to the task (5 files, +673/−0).

- **Slice 3 T3 — control-observation documents** (PR #81, merged `86039ed` 2026-07-31; independent Quality Gate PASS at exact HEAD `65de527`, verdict recorded on the PR ahead of ⟦G-Merge⟧; merge approved without code changes — head SHA and base verified unchanged since the gate): the ADR-0009 control-observation document class — `derive_observed` emits one latest-only document per shipped control (`observed/controls/<control-name>.json`, house shape `{run_id, state, coverage, repositories}`), assembled generically from derived resource-document entries only: entries ascending by repository id in canonical discovery order carrying both plane conclusions and the `{resource, state, reason}` evidence citation (paths never embedded, grep-verified over real emitted bytes at the gate); coverage the descriptor document's block verbatim (qualifies-never-converts pinned through a degraded `incomplete`-inventory fixture); top-level state the Slice 1 listing rule over cited evidence states — evidence-plane rollup only, no operational-state or applicability rollup anywhere; table-order orchestration with per-target resolved accumulation realizing the T2-carried contract, chained conclusions received from predecessors (reversed-evaluation-order detection proven empirically at the gate); the descriptor/control pairing invariant fails loudly (`ValueError`, test-pinned) with the three historical reduced-table test worlds modeling their eras faithfully. **Production diff `src/collector/derive.py` only** (+65/−12 net, 239 lines); all eleven other production modules byte-identical to pre-merge main. Verification chain: S8 validate-mode seam confirmation before any edit; S9 four red-first cycles (cycle 1 genuine red — 4/4 FileNotFoundError, reproduced against merge-base source at the gate; cycles 2–3 disclosed first-run greens; cycle 4 a real mid-cycle regression disclosed and fixed); S10 two-axis review run twice to convergence (Standards PASS, Spec/Conformance PASS, zero hard findings; adjudications applied in three commits — loud pairing guard, V58 comment de-overclaimed, V60 pinned against both committed validation-run trees with real pre-T3 bytes byte-identical, degraded-coverage fixture; mutation demonstrations recorded: broken rollup killed by the V58 test, broken degradation by the V53 test); S11 independent Quality Gate PASS with merge recommendation (all 21 items confirmed, load-bearing ones empirically — T5 definition-only additivity proven with the real push-protection definition through the unmodified derive, committed-tree byte-stability re-executed, mutation kills re-run; 307/307 at HEAD vs 289/289 at merge base, both reviewer-executed; zero Repository Inconsistencies, zero Architecture Proposals). Carried notes — non-blocking, authorizing no work: the stale-`observed/controls/`-document parity note (control removal mirrors descriptor-removal semantics) → post-slice consolidation gate. Test growth 289 → 307 (18 new, zero regressions). Validation on merged main 307/307 tests OK (zero regressions); diff scoped to the task (6 files, +529/−12).

- **Slice 3 T4 — per-control report aggregates** (PR #83, merged `1f55fe0` 2026-08-13; independent Quality Gate PASS at exact HEAD `68b7c09`, verdict recorded on the PR ahead of ⟦G-Merge⟧ with an unconditional merge recommendation and **zero required corrections**; owner-approved merge): the specification Reporting section's per-control distribution aggregates (V59) — **exactly the four count families** (applicability counts, applicability-reason counts, operational-state counts, operational-state-reason counts) as the pure `summary.control_aggregates` function over control-observation documents: closed-vocabulary state gating with atomic (state, reason) pair suppression (an out-of-vocabulary state suppresses its paired reason; a valid state with a junk-typed reason counts as a state figure only), observed keys only (never zero-filled vocabulary maps), type-deep tolerant (malformed control-document inputs never crash aggregation; junk-typed values contribute nothing; valid+junk aggregates byte-identical to incremented valid-only); `derive.run_summary` orchestration deriving resource documents once and feeding both the per-descriptor aggregates (`_fanout_summary` narrowed to consume prepared documents; aggregation loop unchanged) and the T3 control-document generator; `report.py` carrying the summary block verbatim under the additive `controls` key with one bounded Markdown row per control, placed between per-resource aggregates and execution. **Additive-only and estate-independent, test-pinned and gate-reproduced**: the sole JSON delta is the `controls` key and the sole Markdown delta the controls section (the gate's cross-version probe executed merge-base and HEAD code on an identical fixture tree), report size equal across 2- vs 12-repository estates, repeated generation byte-identical, and synthetic-second-control additivity with the first control's aggregate bytes unchanged (T5's definition-only criterion proven forward at the report seam). Verification chain: S9 four red-first cycles (`4463b7e`, `d3a35c7`, `830d792`, `8d779cd`; reds observed, first-run greens disclosed); S10 two-axis review (Standards; Spec+Conformance) run to convergence — the five carried S9 judgement calls adjudicated (atomic plane-unit gating; behavioral-domain test-module split under the spec's "at minimum" list; four families exhaustive per the Reporting enumeration and ADR-0009's rejected list; `_fanout_summary` signature and Markdown placement as local design), the reason-vocabulary type-gating adjudicated conforming (the T7-accepted terminations pattern; reason closure by construction of the control layer; vocabulary reification deferred with the closed-vocabulary-ownership work, #60), two comment-only de-overclaim commits applied (`f8119cf`, `68b7c09` — independently confirmed zero executable lines); S11 independent fresh-context Quality Gate PASS (all 16 falsification attempts failed; suites reviewer-executed — focused 14/11/8/7, full 332/332; environment disclosed: WSL Python 3.14.4 against the recorded 3.12.10 baseline, skew assessed non-verdict-affecting for the stdlib-only suite). Carried forward — all non-blocking, authorizing no work: the S11 editorial observation (the Markdown-preservation `assertIn` membership assertions would not detect a collapsed duplicate line; practically inert beside the paired JSON byte-identity and line-count pins) and the S10 consolidation inputs (the derive.py document-assembly duplication between `run_summary` and `derive_observed`, carried alongside the #59 traversal-unification theme; reason-vocabulary reification, with #60). The owner-clarified product objective (corporate security standard → expected repository control state → observed repository control state → conformance determination → actionable gap) is carried as an owner-directed input to the post-slice consolidation gate feeding the policy-definition decision (ADR-0008's prerequisite for the Policy Expectation and Conformance planes; ADR-0001's "governance product" phrasing to be reconciled there) — a planning carry only, authorizing no implementation. Test growth 307 → 332 (25 new, zero regressions). Validation on merged main 332/332 tests OK (zero regressions); diff scoped to the task (7 files, +568/−18; all ten other production modules byte-identical to the pre-merge main).

- **Slice 3 T5 — `secret-scanning-push-protection` control (additivity proof)** (PR #85, merged `00ab0e8` 2026-08-15; independent Quality Gate PASS at exact HEAD `d3905aa`, **unqualified** with explicit MERGE recommendation, verdict recorded on the PR ahead of ⟦G-Merge⟧; owner-approved merge): the second shipped Security Control, introduced as **its control definition alone** — production diff `src/collector/controls.py` only (+13/−1: the `PUSH_PROTECTION` definition, its registration after `SECRET_SCANNING`, a why-comment) — **empirically proving the accepted ADR-0009 same-evidence-surface additivity claim** (grill condition 1; the cross-evidence-shape proof remains assigned to the next roadmap control's slice and is not claimed). No new API request or descriptor (the shared `security-and-analysis` surface projected both status fields since T1 by design); zero changes to `derive.py`, `summary.py`, `report.py`, `resources.py`, `projections.py`, or any other engine module (all twelve verified byte-identical to merge base `a42772a` by blob hash); applicability chained on Secret Scanning **availability** through the existing T2 chain evaluation and T3 table-order per-target resolved machinery; the control-observation document (`observed/controls/secret-scanning-push-protection.json`) and per-control report aggregates emerged automatically through unmodified T1–T4 machinery. Two-world proof (pre-T5 table vs shipped, committed in-suite and gate-reproduced on fresh reviewer-authored fixtures, 40/40 checks): every pre-T5 observed artifact byte-identical except the one additive Push Protection document; Secret Scanning document and aggregate bytes unchanged; execution/planned-request accounting unchanged; Markdown gained exactly one additive row; deterministic in-process and cross-process. Historical committed validation trees (20260725, 20260730) byte-stable, with every Push Protection conclusion degrading honestly (`unknown` · `evidence-unavailable`, `applicability-unknown` · `secret-scanning-availability-unknown`, citation `raw-evidence-absent`) — absent evidence never invented disablement. Verification chain: S8 validate/design (owner-accepted, no halt conditions); S9 four bounded red-first cycles (`f6c9847`, `f6f24c8`, `7df9f63`, `d3905aa`; cycle 1 genuine red at the shipped-table contract — 1 failure + 2 errors, independently reproduced at S10 against an `a42772a` worktree; cycles 2–4 disclosed first-run greens); S10 three-axis fresh-context review (Standards; Specification; Conformance/additivity-falsification) all PASS, converged with zero required corrections and zero S10 commits — the six cycle-1 stale-expectation updates verified as pure pin-widening, no test weakened; S11 independent fresh-context Quality Gate **PASS unqualified** (reason literals probed for exact spec equality; `unavailable` unreachable across a 160-combination sweep; 16 adversarial resolved shapes degraded correctly; **seven adversarial mutation probes all detected** — broken chain applicability, wrong status-path mapping, unregistered definition, missing document emergence, missing aggregate/report emergence, hidden engine coupling corrupting Secret Scanning bytes, reason-literal drift — proving the disclosed first-run greens meaningful additivity evidence, not tautology). Chain rows V55/V56 and the V45/V46 offline analogs pinned with the real shipped definition; **live V45/V46 acceptance remains T6's responsibility** — no live validation claimed. Carried forward — all non-blocking, authorizing no work: the pre-existing T4-era `mixed_estate` docstring imprecision, the Windows MAX_PATH sensitivity of committed validation-run trees in deep checkouts, and the rare `test_taxonomy` fake-server socket flake (environment/editorial notes for the post-slice consolidation gate); reason-vocabulary reification stays deferred with #60. Test growth 332 → 347 (15 new, zero regressions). Validation on merged main 347/347 tests OK (zero regressions); diff scoped to the task (7 files, +380/−30; every other production module byte-identical to pre-merge main).

- **Slice 3 T6 — fixtures + live validation run** (PR #87, merged `e7ad46e` 2026-08-19; independent Quality Gate **PASS unqualified** at exact HEAD `ce38ff2` with explicit MERGE recommendation, verdict recorded on the PR ahead of ⟦G-Merge⟧; owner-approved merge; validation/evidence-only ticket, **zero `src/` or `tests/` changes** — tree hashes identical at merge base and HEAD, gate-verified): the Slice 3 validation run `20260816T134120Z` committed self-contained under `docs/validation/runs/20260816-GHScannerLab/` (19 raw artifacts, 7 observed documents including both control-observation documents, JSON+Markdown reports), implementation scope `docs/validation/` only (29 files). **Live fixture provisioning** executed under owner authorization per the approved revised plan: the V42 surface probe on all five fixtures gated any mutation; `standard-repo` SS→enabled then PP→enabled in the platform's documented coupling order and `unprotected-repo` SS→enabled with PP deliberately left disabled, each transition verified by independent post-change observation, both post-provisioning invariant predicates verified true, the three untouched fixtures re-observed as affirmative-disabled pins; the two verification contexts (organization-admin mutation effectiveness vs standing-credential row acceptance) kept distinct and non-substitutable; **the scanner remained GET-only and provisioning remained owner/admin activity** — no write capability or provisioning automation entered the repository. **Mandatory rows V41–V47 and V62 accepted from retained live evidence** with the exact accepted literals (V42 dedicated-request surface pinned, fork included; V43 three affirmative-disabled pins for both controls, never from absence; V45 self-evidencing PP applicability with the coupled-transition record; V46 the distinguishable pair live — PP `disabled` · `affirmative-status-disabled` while `applicable` · `secret-scanning-available`; per-control aggregates independently recomputed at the gate: SS enabled 2/disabled 3, PP enabled 1/disabled 4). **V48/V49 retained as authorized recorded gaps** (V10/V12 mechanics; restricted PAT and private fixture not provisioned, scenarios carried by the offline suite); **V61 optional and supported without overclaim** (same-run cross-source agreement independently confirmed divergence-free, 84 shared keys per repository). Integrity: **deterministic destructive rederivation and byte-identical report reconstruction** proven at S9 and independently reproduced twice at the gate (all 7 observed documents and both report formats; the S9 one-off rebuild mismatch confirmed a harness-script defect, not a repository artifact); **clean full-change-set token scan** under multiple independent implementations (29/29 blobs, literal-token check non-vacuous, benign prose hits shown in context); **prior committed validation trees byte-untouched**; `validation-environment.md` updated in the same change set (inventory, run record with gap records, enablement-operation record, V09 credential record, live-coverage delta). Verification chain: S9 (owner-approved plan with two revisions → gate probe → provisioning → collect+derive → integrity proofs → single evidence commit); S10 three-axis fresh-context review (Standards; Specification; Conformance) all PASS, converged, zero corrections; S11 independent Quality Gate PASS unqualified — all twelve falsification targets survived. Carried forward — all non-blocking, authorizing no work: the optional V45 rejection-probe strengthening (owner-level Architecture Proposal, only if ever wanted); the derive/report regeneration cleanup already associated with [#61](https://github.com/a24577t/GitHubScanner/issues/61); the raw `permissions` blocks and ~175 KB `/meta` payload size observation (verbatim append-only retention per ADR-0001, accepted by design). Validation on merged main 347/347 tests OK (zero regressions); diff scoped to the task (29 files, +1269/−5, all under `docs/validation/`).

## Architecture

- **Architecture Baseline:** none published (Pre-Baseline; Slice 1 is implementation of accepted ADRs, not a baseline).
- **Architecture Version:** none.
- **Domain Model / CONTEXT.md:** created (Slice 3 domain modeling, 2026-07-30; scope constraint honored — ADR-0008 domain concepts only).
- **ADRs:** 0001–0009 accepted (`docs/adr/`): three evidence layers + derivability; seven-state taxonomy + affirmative absence; explicit environment targeting; repository resource observation model; canonical target discovery + evidence addressing; taxonomy evidence rules; bounded execution waits; Security Control architectural seam (ADR-0008; detail in the consolidation completed-work entry above); Security Control realization model (ADR-0009: dedicated-request descriptors, control-definition layer, control-observation documents, control-plane degradation rules).
- **MADRs:** 0001–0004 accepted (`.ai/repository/methodology/adr/`); MADR-0004 (2026-07-30) fixes the canonical persistence model for review findings — architectural decision only, implementation vehicle deliberately unselected.
- **Planning:** Security Controls capability roadmap (`docs/planning/security-controls.md`) — eight enumerated Security Control seams, slice assignments TBD at future decision phases.

## Authority Domains

- **Collaborator** — `.ai/collaborator/` (bootstrap, contract, avatar, avatar generator).
- **Repository owner** — `.ai/repository-owner/` (bootstrap, operating guide, repo-owner skill governance).
- **Repository methodology** — `.ai/repository/methodology/` (principles, lifecycle model, glossary, MADRs, Skill Execution Map, transition prompts — skills execute, Repository Gates authorize).
- **Repository state** — `.ai/repository/state/` (this Status Artifact; Repository Continuity Artifact when emitted).
- **Repository history** — `.ai/repository/history/` (immutable evolution evidence; never loaded at bootstrap, never authority).
- **Agent configuration** — `CLAUDE.md`, `docs/agents/` (issue tracker, triage labels, domain-doc rules).

## Next Milestone

Roadmap, in accepted order:

1. S8–S11 for the final Slice 3 ticket (#73, T7 slice completion) → ⟦G-Merge⟧ → Milestone-Complete evaluation → post-slice consolidation. T7 has not begun; Slice 3 is not Milestone-Complete until T7 completes its own verify chain.
2. Later slices select from the Security Controls roadmap (Code Scanning awaits the combination-semantics decision — ADR-0004 deferral, candidates #28/Code Scanning; organization-level Security Configuration awaits the Observation Target Model decision, #26; conformance planes await the policy-definition decision; the cross-evidence-shape additivity proof is assigned to the next implemented control's slice). Wayfinder #2/#3 facts gate GHAS applicability knowledge and the deferred `unavailable` discriminator (trial route expiry 2026-08-28 — scheduling input only).
3. Cleanup tickets #59 (traversal unification), #60 (closed-vocabulary ownership), #61 (offline report regeneration) — normal S6→S7 path, only when owner-scheduled.
4. Observer and Review Evidence Package (REP) implementation under [issue #34](https://github.com/a24577t/GitHubScanner/issues/34) — only when its recorded trigger and explicit repository-owner authorization are satisfied; Architecture Accepted / Implementation Deferred until then.
5. Wayfinder discovery tickets (platform, licensing, native controls, CI/runners, estate shape) may proceed in parallel where already authorized by repository artifacts.
